<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    x:Class="MyWay.Passport.Mobile.Pages.MainPage"
    IsBusy="{Binding IsBusy}"
    Title="MyWay Balance">
    <ContentPage.ToolbarItems>
        <ToolbarItem
            Text="Card"
            Command="{Binding OpenSettingsSelected}" />
    </ContentPage.ToolbarItems>

    <NavigationPage.TitleView>
        <StackLayout
            Orientation="Horizontal"
            VerticalOptions="Center"
            Margin="10,0,0,0"
            Spacing="0">
            <Button
                Text="History"
                Command="{Binding OpenRecentTripsSelected}"
                HorizontalOptions="Start"
                BackgroundColor="Transparent"
                TextColor="{StaticResource LinkTextColor}"
                FontSize="17" />
        </StackLayout>
    </NavigationPage.TitleView>

    <ContentPage.Content>
        <RefreshView
            IsRefreshing="{Binding IsBusy, Mode=OneWay}"
            Command="{Binding RefreshBalanceSelected}"
            BackgroundColor="{StaticResource SecondaryBackgroundColor}">
            <ScrollView BackgroundColor="{StaticResource BackgroundColor}">
                <StackLayout
                    Spacing="70"
                    VerticalOptions="Center"
                    HorizontalOptions="Fill">
                    <Label
                        Text="{Binding CardDetails.LastBalance, StringFormat='${0:F2}'}"
                        FontSize="80"
                        HorizontalTextAlignment="Center"
                        TextColor="{StaticResource TextColor}">
                        <Label.Triggers>
                            <DataTrigger TargetType="Label" Value="True">
                                <DataTrigger.Binding>
                                    <Binding
                                        Path="CardDetails.LastBalance"
                                        Converter="{StaticResource LessThanConverter}">
                                        <Binding.ConverterParameter>
                                            <sys:Double>0.0</sys:Double>
                                        </Binding.ConverterParameter>
                                    </Binding>
                                </DataTrigger.Binding>

                                <Setter Property="TextColor" Value="{StaticResource ErrorTextColor}"/> 
                            </DataTrigger>
                        </Label.Triggers>
                    </Label>

                    <Label
                        Text="{Binding ErrorMessage}"
                        HorizontalTextAlignment="Center"
                        TextColor="{StaticResource ErrorTextColor}" />

                    <Label
                        Text="{Binding CardDetails.LastUpdated, Converter={StaticResource TimeAgoDateConverter}, StringFormat='updated {0}'}"
                        HorizontalTextAlignment="Center"
                        TextColor="{StaticResource SecondaryTextColor}"
                        IsVisible="True">
                        <Label.Triggers>
                            <DataTrigger TargetType="Label" Binding="{Binding CardDetails.LastUpdated, Converter={StaticResource IsNullConverter}}" Value="True">
                                <Setter Property="IsVisible" Value="False"/> 
                            </DataTrigger>
                        </Label.Triggers>
                    </Label>
                </StackLayout>
            </ScrollView>
        </RefreshView>
    </ContentPage.Content>
</ContentPage>
